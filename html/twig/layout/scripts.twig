<script>
    var jsonendpoint = {
        get_test: {
            url: '.',
            method: 'get',
        },
        post_test: {
            url: '.',
            method: 'post'
        }
    };
</script>
{% if debug %}
    {% for file in listJsVendors %}
        <script src="js/vendors/{{ file }}"></script>
    {% endfor %}
{% else %}
    <script src="scripts/vendors.min.js"></script>
{% endif %}
<script src="scripts/bundle.js"></script>
<script>
    // only for debug
    $.get('inc/debug.last-modified.php').promise().done(function (r) {
        var buildTime = r * 1000;
        var now = new Date().getTime();
        console.info('last build', (now - buildTime) / 1000, 'seconds ago');
    })
</script>